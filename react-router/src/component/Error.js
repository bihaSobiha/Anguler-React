import React from "react";

const Error = () =>{
      return(
        <div>
          <p data-myattribute = "somevalue">Error:Path does not exist</p>
        </div>
      );
    }

    export default Error;